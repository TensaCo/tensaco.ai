import type { Metadata } from 'next'
import { Receipts } from '@/components/launch/Close'

export const metadata: Metadata = { title: 'PHASER — methods & sources' }

export default function Notes() {
  return <Receipts />
}
